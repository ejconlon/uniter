module Uniter.Reunitable.Driver where

-- TODO

-- reuniteTerm :: (Recursive t, Base t ~ f, Reunitable f g h m, Alignable e g) => t -> UniterT g m ()

-- reuniteResult :: (Recursive t, Base t ~ f, Reunitable f g h m, Alignable e g) => t -> m (PreGraph g, ReuniteResult e g u h)
-- reuniteResult = undefined -- driveReuniteResult . reuniteTerm
